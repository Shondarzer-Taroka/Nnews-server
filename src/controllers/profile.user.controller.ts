import { Request, Response } from 'express';
import { PrismaClient } from '@prisma/client';


const prisma = new PrismaClient();


export const singleUserInfo=async (req:Request,res:Response) => {
    try {
        
    } catch (error) {
        
    }
}